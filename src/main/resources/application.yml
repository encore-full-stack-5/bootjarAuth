spring:
  application:
    name: bootjarAuth
  datasource:
    url: jdbc:mysql://localhost:3306/users
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret:
  expiration: 6000000000000